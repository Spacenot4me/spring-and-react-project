server:
  port: 666

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/dog
    username: satan
    password: shluxa
  jpa:
    hibernate:
      ddl-auto: create-drop
#      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true

  main:
    web-application-type: servlet